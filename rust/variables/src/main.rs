fn main() {
    // const x = 5 by default
    let x = 5;
    println!("The value of x is : {}", x);
    // Error cuz x is immutable by default
    // immutable: the value can't be mutated / nilainya tidak bisa berubah
    // x = 6;
    println!("The value of x is : {}", x);
}
